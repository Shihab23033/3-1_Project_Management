# Local DB configuration (copy values from db.properties.example or set your own)
# Fill these values for local testing
# Note: Do NOT commit production credentials to source control

db.host=localhost
db.port=3306
db.name=birthday_db
db.user=root
db.password=
